//rscp is shortcut to crate structure of js file
import React from 'react';

const Home = props => {
    return (
        <div>
            <p>Home Page</p>
        </div>
    );
};

export default Home;