//rscp is shortcut to crate structure of js file
import React from 'react';

const Login = props => {
    return (
        <div>
            <p>Login Page</p>
        </div>
    );
};

export default Login;